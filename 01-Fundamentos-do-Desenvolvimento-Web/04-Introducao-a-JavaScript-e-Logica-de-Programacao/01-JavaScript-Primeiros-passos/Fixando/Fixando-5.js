let weekDay = "domingo";

if (
  weekDay === "segunda" ||
  weekDay === "terça" ||
  weekDay === "quarta" ||
  weekDay === "quinta" ||
  weekDay === "sexta"
) {
  console.log("dia de trybe");
} else if (weekDay === "sábado" || weekDay === "domingo") {
  console.log("descanso");
}
